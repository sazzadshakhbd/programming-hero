var tomatoPrice = 25; // Variable decleartion
tomatoPrice = 30;// variable value update

// Add something to the previous value
var price1 = 31;
var price2 = 10;

// Set a new value
price1 = 35;

// Add some value to the existing value
price1 = price1 + 10;

// Addition to the same variable short hand
price1 += 10;

// Substraction to the same variable short hand
price1 -= 20;

// Multiplication to the same variable short hand
price1 *= 2;

// Division to the same variable short hand
price1 /= 35;

var age = 39;
age += 1;
age++; // ++ means addign 1 to the existing

var love = 100;
love -= 1;
love--; // ++ means addign 1 to the existing
console.log(love);

