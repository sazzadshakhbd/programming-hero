/*
    Problem-7:

     Write a function named findArea() that will take base and height of a triangle and will return the area of triangle.

*/

function findArea(base, height) {
    const area = 0.5 * base * height;
    return area;
}
var result = findArea(5, 7);
console.log(result);

